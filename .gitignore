# Runtime render cache (regenerated per session, scoped per machine)
cache/

# Logs / scratch
*.log
*.tmp

# HUD runtime state — centralized under <hud>/.claude-statusline/<project-id> by
# default (override with HUD_STATE_DIR). Matched at any depth; never committed.
.claude-statusline/

# Local agent tooling scratch
.omo/
