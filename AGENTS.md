# AGENTS.md

Self-contained Claude Code statusline (HUD). Reads one JSON payload on stdin, prints the status line on stdout. Pure Node built-ins, ESM, **zero dependencies**. See `README.md` for end-user setup/proxy/env docs — this file is the agent-specific map.

**Shared vocabulary lives in [`GLOSSARY.md`](GLOSSARY.md)** — read it if a term here is unfamiliar (element, fragment, palette/token, session key, render context, safeMode, …). It also flags the easy-to-confuse pairs (`statusline` vs `statusLine`, render context vs context window). When you add an element/theme, add its term there too.

## Source of truth & build (read first)

- **The `.js` files under `src/` ARE the source. Edit them directly.** They look like transpiled TypeScript (JSDoc `@param` types, `??`, a comment in `src/hud/types.js` referencing a non-existent `render.ts`), but there is **no `tsconfig`, no `.ts` files, no build step, no `node_modules`**. Do not look for or create a build pipeline; do not introduce `.ts`.
- **No package manager install is needed or wanted.** `package.json` has no deps, no scripts. Never add a dependency or `node_modules` — the project's entire value proposition is being self-contained Node built-ins only (`"type": "module"`, `engines.node >=14.17`).
  - Caveat: `src/lib/atomic-write.js` uses `crypto.randomUUID()`, so the real floor is **Node ≥ 14.17**, not 14.0.
- Shared utilities live in **`src/lib/`** (recently moved from `src/utils/`, `src/cli/utils/`, `src/platform/`). Import helpers from `../lib/...`.

## Verification (there are no tests)

No test runner, no linter, no formatter, no typecheck, no CI. The only verification is the smoke test — **run it after any change**:

```sh
echo '{"session_id":"t","cwd":"'"$HOME"'/example","effort":{"level":"high"},"model":{"id":"claude-opus-4-8","display_name":"Opus 4.8"}}' \
  | HUD_SYNC_REFRESH=1 sh statusline.sh statusline.mjs
```

Expect one line containing `opus:high`. `HUD_SYNC_REFRESH=1` renders *this* payload synchronously and prints that line — without it the wrapper's hot path prints the **previous** frame from the cache and only refreshes in the background, so a code change would look like it had no effect (that was the behaviour until 2026-09, even with the flag); `HUD_DEBUG=1` adds stderr diagnostics. To exercise the renderer alone: `... | node statusline.mjs`. No `session:` fragment appears: the sample payload names no transcript, and the element hides rather than reading `0m`.

## Execution flow (not obvious from filenames)

```
Claude Code → statusline.sh → find-node.sh → statusline.mjs → src/hud/index.js → src/hud/render.js → src/hud/elements/*.js
```

- `statusline.sh` (POSIX, no bashisms) is a **caching wrapper**, not just a launcher. Hot path = `cat` the last rendered line for this session and kick a background Node refresh for the *next* frame. First render per session is **synchronous** because the next trigger can be a long way off: Claude Code runs the `statusLine` command once at session start (resume included) and then only when a new assistant message arrives, `/compact` finishes, the permission mode changes, vim mode toggles, a configured `refreshInterval` elapses, or a rate-limit window / warm prompt cache in the last payload reaches its `resets_at` / `expires_at` — so an async-only first frame would leave `[HUD] Starting...` on screen until the user sends something. Two more consequences of that contract: updates are debounced 300 ms and **an in-flight command is cancelled when a new trigger fires**, which is the other half of why the hot path must stay a `cat`; and with no `refreshInterval` set the triggers go quiet whenever the session is idle (a lead waiting on background teammates is the documented case), so every time-based fragment — `session:`, the limit countdowns — stands still until the session moves. Elements that track live state are therefore viable only with `refreshInterval` (minimum `1` second) configured. **Config edits are picked up on the next frame, not the one after:** the hot path is bypassed (forcing a synchronous re-render) whenever `config.json`'s mtime is newer than the cached `statusline.txt` — see `config_newer_than` (the config path mirrors Node's `getHudConfigFile`: `HUD_CONFIG` override else `<install>/config.json`). When that sync render can't run (lock held / render failed), the wrapper still prefers the stale cached line over the `[HUD] Starting...` placeholder. Two failure shapes, handled differently on purpose: a render that prints **nothing** (crash, timeout, no `node`) keeps the last good line, while one that prints a `[HUD] …` fallback line (a caught runtime error) **replaces** it, because that line points at stderr — kept as `statusline.err` until the next good render. The docs describe `COLUMNS`/`LINES` as set for the command, but a live 2.1.261 hook process had neither, so the `maxWidth` auto-detect in `index.js` (which would wrap the line at separators) does not trigger through the wrapper. All runtime files — the shell's per-session render cache + `mkdir`-based render lock, *and* the Node HUD's state — live in a **per-session subfolder** of `cache/` (gitignored): `cache/<session>/<name>.json` (e.g. `stdin.json`, `statusline.txt`, `render.lock`, `lead-tokens.json`). Keep it relocatable: the cache dir is derived from the script dir (shell) / module location (Node), honors `HUD_CACHE_DIR`, and is never hardcoded.
- `find-node.sh` locates `node` for non-interactive shells (PATH → nvm → fnm → homebrew) and always `exit 0` so it never blocks Claude Code.
- `statusline.mjs` installs an `HTTPS_PROXY`/`https_proxy` CONNECT tunnel by monkey-patching `node:https` **before** dynamically importing the HUD, then `import()`s `src/hud/index.js`. Credentials in the proxy URL become `Proxy-Authorization`, an `https://` proxy is dialed over TLS, and a CONNECT not answered with 200 (407/403) fails the request instead of handshaking on the proxy's error page. Verified against a local authenticating CONNECT proxy (`scratchpad`-style harness, not checked in).
- `src/hud/index.js:main()` builds a single `context` object from stdin + transcript (tail read plus the incremental tally memo) + usage API, then calls `render(context, config)`.

## Configuration

- User config lives in the HUD's **own `config.json`** at the install root (next to `cache/`), resolved by `getHudConfigFile()` in **`src/lib/install-paths.js`** (default `<install>/config.json`; `HUD_CONFIG` env overrides with an absolute path). It is **gitignored** (`/config.json`); the committed **`config.json.example`** is the canonical reference and mirrors `DEFAULT_HUD_CONFIG`. The file is **optional** — absent/unreadable ⇒ code defaults. Its top-level object **IS** the config (no wrapper key). `src/hud/state.js#readHudConfig()` reads it with a plain `JSON.parse`; `src/hud/themes.js#readConfiguredTheme()` separately reads just `theme` from the same file at import. Precedence: `config.json` > defaults (theme also honors `HUD_THEME` env first).
- **Why its own file, not Claude Code's `settings.json`:** the HUD historically read a lowercase `statusline` block from `settings.json`, but that is **distinct** from the `statusLine` (capital-L) command-hook key (README). Claude Code's settings schema is a non-strict Zod object that **silently strips unknown keys** — so any non-schema config there (a lowercase `statusline` block, or anything nested inside `statusLine`) survives the HUD's own parse but gets wiped whenever Claude Code's settings tooling rewrites the file. `config.json` sidesteps that entirely. (The `statusLine` hook schema accepts only `type`, `command`, `padding`, `refreshInterval`, `hideVimModeIndicator`.)
- Defaults, labels (en + zh-CN), and the canonical element order (`DEFAULT_ELEMENT_ORDER`) all live in **`src/hud/types.js`**. Change defaults there — and keep `config.json.example` in sync.

## Adding or changing a statusline element

An element is a pure function `export function renderXxx(args) → string | null` (return `null` to render nothing; the string is a fragment with **no separators and no newlines** — `render.js` joins fragments with `" | "`). The HUD renders a **single line**, so every element is an inline fragment.

To add one, touch these files (in order):
1. `src/hud/elements/<name>.js` — the render function. Use color helpers from `src/hud/colors.js` (`paint`, `paintLabel`, `paintFaint`, `PALETTE.*`), never raw escape codes.
2. `src/hud/render.js` — import it, guard on `enabledElements.<name>`, and store the result via `rendered.set("<name>", el)`.
3. `src/hud/types.js` — add the enable flag to `DEFAULT_HUD_CONFIG.elements` and place `"<name>"` in `DEFAULT_ELEMENT_ORDER.main`.
4. `src/hud/index.js` — only if the element needs new data: extract it and add a field to the `context` object.

## Adding or changing a theme

- Palettes (the color **data**) live in **`src/hud/themes.js`** as the `THEMES` registry; **`src/hud/colors.js`** is the rendering **engine** (color-depth detection, `fg`/`paint`, gradient math) and exports the active palette as **`PALETTE`**. To add a bundled theme, add one entry to `THEMES` with all 10 tokens as **`#rrggbb` strings** (the file header documents each token's role) — **no other file changes**. Every element already routes through `PALETTE`/the `paint*` helpers.
- **Hex is the authoring format, `[r,g,b]` the runtime one.** `THEMES` entries and `config.json` palettes are both `#rrggbb` — deliberately identical, so a bundled and a user palette are the same shape, and a hex comment can't drift from an array. `resolvePalette` converts to the `[r,g,b]` triples `fg()` takes exactly once, at the end; `PALETTE` and every element are unchanged by this. Only `#` plus exactly 6 hex digits parses — no `#rgb`, no names, no arrays.
- Active theme is resolved **at import** in `themes.js#resolveThemeName`: `HUD_THEME` env > `config.json` `theme` > `DEFAULT_THEME` (`aurora`). It must be import-time, not render-time, because elements freeze `fg(PALETTE.x)` into module-level constants before `main()` reads the runtime config. That is also why `themes.js` keeps its own minimal `JSON.parse` of `config.json` (routing through `state.js#readHudConfig` would risk an import cycle) — **one** parse now yields both `theme` and `themes`. `HUD_THEME=<name>` is the quick way to A/B a render, and it searches the *merged* registry, so it selects user themes too.
- **User themes (`config.json` `themes`) are colors only** — a partial or complete palette plus a reserved `base`. Deliberately no glyphs, no separator, no tier bounds: `sep`/`░` and ` | ` stay hardcoded (and `safeMode` rewrites the bar glyphs in `sanitize.js`), and the 70/85 cut points stay in `TIER_BOUNDS`/`thresholds` so a color can never disagree with the `COMPRESS?`/`CRITICAL` text beside it.
- Three resolution rules carry the weight, all in `resolveHexPalette`:
  - **Unset tokens come from `base`**, recursively. `base` defaults to the theme's *own* name when it shadows a bundled palette and to `DEFAULT_THEME` otherwise — defaulting it unconditionally to `DEFAULT_THEME` silently broke retinting for every bundled theme except aurora (`themes.ember = { label }` rendered *aurora* with one changed token, and nothing warned, because every name involved was valid). This is what makes a 1-token theme safe: a missing token would otherwise reach `fg(undefined)`, throw, and blank that fragment — the per-element catch turns a typo into silence, so the merge is the real guard, not the debug warning.
  - **A name already being resolved falls back to the *bundled* palette of that name.** One rule, two cases: `themes.aurora` whose `base` defaults to itself (a retint — the intended way to recolor a bundled theme in place), and a genuine `a → b → a` cycle. `MAX_BASE_DEPTH` backstops it.
  - **A malformed value keeps the inherited color** and warns under `HUD_DEBUG`; it never invalidates the whole theme. Same for unknown token keys.
- **Registry lookups are own-property only, and the registry has a null prototype.** Both matter, and both were bugs first: with plain `obj[k]`/`in`, a theme or `base` named `constructor` read as an existing palette and suppressed the unknown-name warning; and because `JSON.parse` makes `__proto__` a real own key, `themes[name] = value` on a `{}` registry invoked the inherited setter and re-pointed the registry's prototype, changing lookups for every *other* name. Use `has()` and `Object.create(null)`, never bare `in`.
- **Keys inside a theme are matched case-insensitively** through `CANONICAL_KEY`, like theme names — including `base`, which is why `readBase()` scans the keys instead of reading `user[BASE_KEY]`. A literal read accepted `"base"` but let `"Base"` fall through to the token loop, so the theme silently inherited the wrong palette while the only warning named a "token" that isn't one.
- **`preview-themes.mjs` (repo root) renders every theme — bundled *and* user-defined — in one process**, which is only possible because `resolvePalette(name)` is exported: `PALETTE` is frozen at import, so a process has exactly one *active* theme. It paints through the real `fg()`, so a preview cannot drift from a render; the sample *values* are hard-coded, so it is representative, not live. `--depth=0|1|2` re-execs itself with a doctored `TERM`/`COLORTERM` rather than second-guessing the detection in `colors.js`. Theme one-liner descriptions live in that script, not in `THEMES` — an 11th key would trip the parity check and `THEME_TOKENS`.
- Built-in themes: `aurora` (cool slate, the original — current default), `ember` (warm gruvbox), `nebula` (vivid Catppuccin-Mocha mauve), `graphite` (near-monochrome), `daylight` (the only one built for a **light** terminal background). New palettes were checked two ways before landing: WCAG contrast against the theme's intended background (`sep` is exempt — it is a hairline and a gauge track), and whether `rgbTo256`/`rgbTo16` keep tokens distinct after degradation. Counting every token pair whose source hex differs: at **256 colors** only `aurora` collides at all (`label`/`track`, both index 153); the other four are clean. At **16 colors** every theme collides, the originals worst of all — `aurora` 7 pairs, `ember` 8, against `nebula` 1 (`text`/`faint`) and `daylight` 2. `graphite` has 21, i.e. everything but the ramp goes white: accepted rather than fixed, because saturating those tints enough to survive `rgbTo16`'s `max - min < 40` neutral test would defeat the theme, and the `+`/`!`/`?`/`✗` glyphs still differ. Note `rgbTo16` also sends any near-neutral color with `max <= 120` to **black**, which on a dark terminal is invisible — that, not distinctness, is why graphite's `faint` is `#7d7d7d` and not darker. `DEFAULT_THEME` lives in `themes.js`; `THEME_TOKENS` derives the canonical 10 names from it, so the token list can't drift. The documented config defaults (`theme`, `themes`) live in `DEFAULT_HUD_CONFIG` (`src/hud/types.js`) and are **doc-only** — `mergeWithDefaults` drops both. Keep `config.json.example`'s `themes.custom` entry spelling out all 10 tokens.

## Conventions & gotchas

- **Rendering must never throw to the user.** Every layer swallows errors; an uncaught one becomes the `[HUD] HUD error - check stderr` line (stack in `statusline.err`), and the shell keeps the last good line only when the renderer printed nothing at all. Match this: wrap risky work in try/catch, fall back to `null`/empty, and gate noisy logs behind `HUD_DEBUG`.
- **State writes go through `atomicWriteJsonSync` (`src/lib/atomic-write.js`).** Never `fs.writeFileSync` a state/cache file directly — atomicity (temp + `fsync` + rename + dir `fsync`) is required because concurrent sessions race.
- **Cross-process coordination uses `src/lib/file-lock.js`** (`withFileLock`, `lockPathFor`). Lock files sit next to the data file and carry a PID; a lock is stale only when older than the threshold **and** its PID is dead.
- **State is centralized, never written into the user's project.** All runtime files live under one cache dir — by default the HUD install's own `cache/` (derived from the module's location in `getCacheDir`, so it follows a relocated install); `HUD_CACHE_DIR` overrides it and the shell wrapper honors the same var, so the two layers never diverge. Each session gets its **own subfolder** named by the (sanitized) session id, and the files inside drop the suffix: `cache/<session>/hud-stdin-cache.json`, `.../compact-requested.json`, `.../subagent-tokens.json`, `.../lead-tokens.json`, `.../svn-status.json` (plus the shell's `stdin.json`, `statusline.txt`, `render.lock`). There is no `hud-state.json` any more — the session start it persisted is part of the `lead-tokens.json` memo. The globally-unique session id naming the folder keeps unrelated sessions **and** projects from colliding, so there is no per-project subdirectory (and the stdin/stabilization cache can no longer be clobbered across sessions). Always resolve via `src/lib/worktree-paths.js` (`getSessionCacheDir`, `sessionCacheFile`, `ensureSessionCacheDir`); never hardcode. The session key is derived once in `index.js#resolveSessionKey` (`HUD_SESSION_KEY`, the key statusline.sh computed and exported so the two layers can never diverge, even on its checksum fallbacks → stdin `session_id` → session-id env vars → transcript UUID → `default`) and threaded into every cache read/write. Session folders idle longer than `HUD_CACHE_MAX_AGE_DAYS` (default 14) — and legacy flat cache files — are pruned at most once a day from the shell's locked refresh path.
- **Width math uses `stringWidth()` from `src/lib/string-width.js`, not `.length`** (ANSI is stripped first; CJK counts as 2). Emoji/ZWJ width is approximate.
- **`safeMode` (default `true` on every platform; an explicit `false` disables it, Windows included) changes output:** it strips non-SGR ANSI and swaps Unicode bar glyphs for ASCII via `src/hud/sanitize.js`. Outside safe mode, `index.js` replaces spaces with non-breaking spaces (`\u00A0`) for alignment — keep both paths working.
- **Usage/rate-limit data (`src/hud/usage-api.js`)** reads OAuth creds from the macOS Keychain or `~/.claude/.credentials.json`, calls `api.anthropic.com/api/oauth/usage`, and caches per-provider with backoff. It is intentionally suppressed when `ANTHROPIC_BASE_URL` points at a non-Anthropic gateway. **Credentials are strictly read-only — the HUD never refreshes an expired token.** OAuth refresh tokens are single-use, so a refresh from a hook revokes the access token Claude Code itself holds and forces a re-login (anthropics/claude-code#42603, closed with "remove all token refresh logic from hooks"); the old refresh + write-back was exactly that. An expired token yields `error: 'auth'`: the cached Opus/Sonnet weekly and `extra:` buckets are served while under 15 min old, then `[API auth]`, until Claude Code rewrites the store on its own (the `auth` cache TTL is 15 s, so it is re-read promptly). Current Claude Code puts `five_hour`/`seven_day` in the payload's `rate_limits`, which take precedence in `mergeStdinRateLimits` and are live and unmarked throughout — that merge also drops the API's `stale` flag, because the `*`/`~` marker is per fragment and would otherwise paint the payload's fresh numbers; only when the payload carries no `rate_limits` at all (older Claude Code) are the API's numbers rendered with the marker. When testing the endpoint by hand, set `HTTPS_PROXY` exactly as the live session has it: from this machine a **direct** egress gets `403 forbidden / Request not allowed` (region block), and only the tunnel reaches it — which is also why a local test proxy that connects upstream directly can never observe a 200.
- **`token:` folds in teammate/subagent tokens (`src/hud/subagents.js`).** Claude Code writes each subagent to its own transcript under `<lead-transcript>/subagents/` (sharing the parent `sessionId`), which the lead transcript `parseTranscript` reads never contains — so `sessionTotalTokens` would undercount every multi-agent run (often ~half). **There are two layouts, and the walk must stay recursive to catch both:** Agent-tool teammates land flat at `subagents/agent-*.jsonl`, but Workflow-tool ("ultracode") agents nest one level deeper at `subagents/workflows/wf_<id>/agent-*.jsonl`. A flat `readdir` sees only the `workflows` *directory*, which fails the `agent-` prefix test — so every workflow run scored 0 and hid the bulk of a session's tokens (measured: 667k of 872k on a real 30-agent run). Recursion is hand-rolled: `readdirSync`'s `recursive` option is Node 18.17+ and this project floors at 14.17. Descent is bounded by `MAX_WALK_DEPTH` and follows `isDirectory()` (false for symlinks, so cycles can't trap it). `index.js` adds `sumSubagentTokens(resolvedTranscriptPath, sessionKey)` to the lead total from `tallyLead(...).totalTokens` (null stays null → element still hides). Per-file tallies are memoized on disk (`subagent-tokens.json`, a `tallyFile` memo plus a size+mtime stamp, under each file's path *relative to* the subagents dir) because the one-process-per-render model kills in-memory caches; an untouched team costs one `stat()` per file. Fails to 0 on any read error, and a single vanished/rotated file keeps its last good sum rather than zeroing the team — **which works only because the fast path also requires `hit.consumed === file.size`**. `tallyFile` signals failure by returning a memo short of the file end; without that check one unreadable frame would be stamped as authoritative and, since a finished transcript never changes size or mtime again, freeze that teammate at 0 for the whole session (reproduced, then fixed). `MAX_SUBAGENT_FILES` (512) is a backstop, not a budget — one workflow can spawn 1000 agents, so if it ever bites it sums the *largest* files and says so under `HUD_DEBUG`. **Both sides count through `src/hud/token-tally.js` (`tallyFile`), never by summing rows.** Two bugs forced this and both are fixed there, so do not reintroduce a naive row sum:
  - *One API call writes several rows.* Claude Code emits one row per assistant **content block** (`thinking`, `text`, each `tool_use`), all sharing one `message.id`/`requestId` and each carrying that call's usage. Summing rows counted a call 2–4×. `tallyFile` de-duplicates on `message.id`, **last row wins — and the reason matters**: the rows of a group are *not* identical (10,178 of 35,751 groups across 509 transcripts differ per row; early rows can hold a placeholder, e.g. `output_tokens` `3, 3, 6205`), but they are **monotonically non-decreasing** (0 decreasing steps), so last-wins and max-wins agree while first-wins would be badly wrong. Measured across all 157 transcripts on the dev machine: **median 2.46×, max 4.11×** inflation; on the ultracode session that prompted this, 2,825,110 → **2,484,323** (lead alone 379,393 → 161,157). Subagent files inflate far less (~1.05× on that session) than leads (the 2.46× corpus median above; that session's own lead was 2.35×), so a heavy multi-agent run *dilutes* the error rather than showing it.
  - *Tail-reading silently truncated the total.* The old lead path parsed only the last 4 MiB yet published the result as a complete session sum — measured on a 25.4 MB transcript as 739,480 against a true 1,910,334 — **39% of the truth** (14 of 157 leads exceed 4 MiB), and since the window is a fixed byte count, one large `tool_result` could evict usage rows and make the counter run **backwards**. `tallyFile` scans the whole file *incrementally* instead: transcripts are append-only, so each frame parses only the newly appended bytes and resumes a memoized running total (lead memo: `lead-tokens.json`). A full cold scan of that 25.4 MB file costs ~260 ms — far too slow per frame, which is why the memo is load-bearing, not an optimization.
  - Two verified on-disk properties make the incremental design safe: rows sharing a `message.id` are **contiguous** among usage rows (0 exceptions in 73,748 rows / 509 transcripts), so only the last group can straddle a chunk boundary (`lastId`/`lastIdTokens` correct it) — note this fails *silently* in both directions if ever violated, nothing asserts it; and a `message.id` is never shared between **files that get summed together** (0 between a lead and its own `subagents/`, 0 between sibling subagent files, over 56 sessions with teammates) — that, not the `isSidechain` argument, is why lead+subagent addition double-counts nothing. Ids *are* shared more widely — 30 appear in up to 5 files — but only across **forked** sessions, which are separate leads never added to their parent. An FNV-1a `fp` over the first 512 bytes plus an `isLineBoundary` check at the resume offset make it very unlikely that a memo no longer matching the bytes there is trusted — `isLineBoundary` misses only a shift that leaves a `\n` at exactly that offset (a different file in the same slot, or a rewrite that shifts content) and force a rescan; a shrink does too. Compaction is *not* such a case — it appends `compactMetadata` rows to the same growing file.
  - `parseTranscript` therefore computes nothing cumulative: it tail-reads 4 MiB for `lastRequestTokenUsage` only (feeds `ctx:`, wants exactly the last row). The **tool/agent/skill counts and the session start** ride the same tally memo (`tallyLead` → `toolCalls`/`agentCalls`/`skillCalls`/`firstTimestamp`), for the same reason: read from the tail they were truncated on any large transcript (measured on a 4.9 MB one: 442 of 494 tool calls, 2 of 3 agent calls) and ran backwards as the window slid. Counting per row is safe because each assistant row carries exactly one content block (0 rows with several, 0 repeated `tool_use` ids, across 50 transcripts / 16,330 rows). A memo lacking the counters (written before they existed) is not resumable and rescans once — the lead's, and in `subagents.js` any teammate file that has *changed* since (an unchanged one keeps its old memo through the stamp fast path, which is fine: only the lead's counts are rendered); `tallyLead` persists on any field change (`sameMemo`), which is what makes that migration cost one frame instead of a rescan every frame.
- **`gitRepo`/`gitBranch`/`gitStatus` are VCS slots, not git-only elements.** A Subversion checkout gets the same three fragments (`repo:proj | branch:2.1@12345 | ✗2 +2 !3 ?1`) under the same three config keys — there are no `svn*` flags to enable. **`render.js` picks the VCS once** (`useSvn`) rather than per slot: a per-slot `renderGitX(...) ?? renderSvnX(...)` lets the slots disagree, because `renderGitRepo` returns null with no `origin` remote and `renderGitStatus` returns null for a *clean* tree — so a git clone inside an SVN working copy would show the SVN project beside a git branch, and its clean tree would fall through to `svn status`, which reports that directory as unversioned. Git wins ties. The repo name and the worktree suffix come from the payload's `workspace.repo.name` / `workspace.git_worktree` when present (`index.js` → `context.repoName` / `context.worktreeHint`; the hint is trusted only alongside `repo`, since `git_worktree` is the older field — 2.1.97 against ≤2.1.233 for `repo` in the cached payloads), which spares `git remote get-url` and the two `rev-parse` calls: a frame in a git repo spawns 3 git processes instead of 6, `branch --show-current` and `status` being the survivors. Cost otherwise is one `git rev-parse` for the decision plus, on the SVN side, a **filesystem** walk for `.svn` (`findSvnWorkingCopyRoot`, `src/lib/worktree-paths.js`, which `realpathSync`es first so a symlinked cwd still finds the root): a git checkout never spawns `svn`, and an SVN checkout never spawns the three git element commands.
  - `src/hud/elements/svn.js` shells out only after that gate, and only to `svn info|status --xml --non-interactive`: `--xml` avoids the locale-dependent text output, `--non-interactive` makes a credentials prompt impossible, and the **absence of `-u`** keeps both commands local — which is also why SVN has no ahead/behind (`⇡`/`⇣`) counterpart.
  - **Both status columns count.** `wc-status` carries `item` *and* `props`, and reading only `item` scored a property conflict (`item="normal" props="conflicted"`) and a property-only edit as clean — the latter being the normal state of every merge root, since `svn merge` writes `svn:mergeinfo` there. Mapping: `added`/`deleted`/`replaced` → staged (SVN has no index; "staged" means scheduled for commit), `modified`/`merged`/`missing`/`incomplete` → modified, `unversioned` → untracked, `conflicted`/`obstructed`/`props="conflicted"`/any `tree-conflicted="true"` → conflicted, and `props="modified"` → modified but checked last so a path is never counted twice. Note `svn status` descends into externals (one extra `<target>` each), so their changes are counted individually — git would show a dirty submodule as one entry.
  - **`svn status` walks the whole working copy, so its counts are memoized to `cache/<session>/svn-status.json`**, not to the module-level Maps — one process per render kills those, exactly as for `subagents.js`. **The memo has two independent invalidation triggers, ORed, and neither is redundant:** a 30 s TTL, and a change stamp (`mtimeMs:size`) over the working copy's `.svn/wc.db`. On the clock alone an `svn delete` stayed invisible for up to 30 s — reproduced, and the reason the element looked broken. Every *metadata* operation commits a SQLite transaction to `wc.db`, so its mtime is a ~µs proxy for "the schedule changed": **measured** to move for `svn delete`, `add`, `revert` and `propset`, and — load-bearing — **not** to move for `svn status` itself, so a read cannot invalidate its own memo. It equally **measurably does not** move for a versioned-file edit, a plain `rm` (`missing`), or a new unversioned file: those are pure working-file changes touching no metadata, and only the TTL catches them. Same for externals — each carries its own `.svn/wc.db`, and the root's stamp says nothing about theirs. The stamp is read *after* the walk, so a future `svn` that wrote wc.db while reporting status could not put every frame into a rescan loop. No `wc.db` (pre-1.7) ⇒ stamp `null` on both sides ⇒ TTL alone, i.e. the old behaviour. Measured on a 96k-file checkout with 8 externals: `svn status` ~510 ms, a cold HUD frame ~693 ms against ~175 ms on a memo hit. Without the memo every frame re-walked the tree, and on a large checkout that both stalls the synchronous first render and blows the timeout, leaving the fragment empty forever. A rescan that fails or times out serves the last good counts rather than blanking. `svn info` is a single-file read and stays per-frame, which keeps the revision fresh across `svn update`. `execFileSync` also needs an explicit `maxBuffer` here: it **throws** ENOBUFS past 1 MiB, and `svn status --xml` spends ~90 bytes per unversioned path, so an unignored build directory would silently delete the element.
  - `resolveToWorktreeRoot` mirrors the same order (git root → SVN working-copy root → the directory as given). It used to substitute `process.cwd()` for any non-git directory; since `statusline.sh` never `cd`s, that usually *was* the session's cwd, but it diverges whenever the hook is invoked from elsewhere (the smoke test is such a case) — and returning the SVN working-copy root, plus dropping an unconditional stderr line, are wins regardless.
- WSL detection and process-liveness go through `src/lib/platform.js`; plain OS branches (`process.platform === 'win32' | 'darwin'`) still appear inline where simpler.
